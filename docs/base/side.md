# Side
A **Side** is what primarly composes [`Dice`](/docs/base/dice.md), and can have multiple [`Effects`](/docs/base/effect.md) that can be executed.

## Attributes
* **effects**: a list of `Effects` that will be executed in sequence.
* **weight**: a weight to be used when rolling a `Dice` that contains the `Side`.
